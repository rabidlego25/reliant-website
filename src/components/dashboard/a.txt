              {columnData.map((training, idx) => {
                return (
                  <MenuItem key={idx} value={training}>
                    {training}
                  </MenuItem>
                );
              })}